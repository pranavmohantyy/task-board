const addTask = (title, priority = 'low') => {\n    const task = document.createElement('div');\n    task.className = 'task';\n    task.innerHTML = `<span contenteditable='true'>${title}</span> <span class=\"priority\" data-priority=\"${priority}\"><span class=\"dot ${priority}\"></span></span> <button class=\"delete-btn\">X</button>`;\n    task.draggable = true;\n    task.addEventListener('dragstart', dragStart);\n    task.querySelector('.delete-btn').addEventListener('click', deleteTask);\n    task.querySelector('.priority').addEventListener('click', togglePriority);\n    document.querySelector('.column .task-list').appendChild(task);\n    saveBoardState();\n};\n\nconst togglePriority = (e) => {\n    const priorityElem = e.target.closest('.priority');\n    const currentPriority = priorityElem.getAttribute('data-priority');\n    const newPriority = currentPriority === 'low' ? 'medium' : currentPriority === 'medium' ? 'high' : 'low';\n    priorityElem.setAttribute('data-priority', newPriority);\n    priorityElem.querySelector('.dot').className = 'dot ' + newPriority;\n};\n\nconst deleteTask = (e) => {\n    const task = e.target.parentElement;\n    task.classList.add('fade-out');\n    setTimeout(() => task.remove(), 300);\n    saveBoardState();\n};\n