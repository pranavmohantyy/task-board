const addTask = (title) => {\n    const task = document.createElement('div');\n    task.className = 'task';\n    task.innerHTML = `<span contenteditable='true'>${title}</span> <button class=\"delete-btn\">X</button>`;\n    task.draggable = true;\n    task.addEventListener('dragstart', dragStart);\n    task.querySelector('.delete-btn').addEventListener('click', deleteTask);\n    document.querySelector('.column .task-list').appendChild(task);\n    saveBoardState();\n};\n\nconst deleteTask = (e) => {\n    const task = e.target.parentElement;\n    task.classList.add('fade-out');\n    setTimeout(() => task.remove(), 300);\n    saveBoardState();\n};\n\nconst dragStart = (e) => {\n    e.dataTransfer.setData('text/plain', e.target.querySelector('span').textContent);\n};\n\nconst allowDrop = (e) => {\n    e.preventDefault();\n};\n\nconst drop = (e) => {\n    e.preventDefault();\n    const title = e.dataTransfer.getData('text/plain');\n    addTask(title);\n};\n\nconst saveBoardState = () => {\n    // code to save board state to localStorage\n};