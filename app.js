const addTask = (title, priority = 'low') => {\n    const task = document.createElement('div');\n    task.className = 'task';\n    task.innerHTML = `<span contenteditable='true'>${title}</span> <span class=\"priority\" data-priority=\"${priority}\"><span class=\"dot ${priority}\"></span></span> <button class=\"delete-btn\">X</button>`;\n    task.draggable = true;\n    task.addEventListener('dragstart', dragStart);\n    task.querySelector('.delete-btn').addEventListener('click', deleteTask);\n    task.querySelector('.priority').addEventListener('click', togglePriority);\n    updateTaskCount();\n    document.querySelector('.task-list').appendChild(task);\n};\n\nconst updateTaskCount = () => {\n    const todoCount = document.querySelector('#todo-count');\n    const inProgressCount = document.querySelector('#inprogress-count');\n    const doneCount = document.querySelector('#done-count');\n    todoCount.innerText = document.querySelectorAll('.column:nth-child(1) .task').length;\n    inProgressCount.innerText = document.querySelectorAll('.column:nth-child(2) .task').length;\n    doneCount.innerText = document.querySelectorAll('.column:nth-child(3) .task').length;\n};\n// Call updateTaskCount when a task is moved or deleted