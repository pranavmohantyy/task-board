const addTask = (title, priority = 'low', dueDate = 'N/A') => {\n    const task = document.createElement('div');\n    task.className = 'task';\n    task.innerHTML = `<span contenteditable='true'>${title}</span> <span class=\"priority\" data-priority=\"${priority}\"><span class=\"dot ${priority}\"></span></span> <span class=\"due-date\" contenteditable='true'>Due: ${dueDate}</span> <button class=\"delete-btn\">X</button>`;\n    if (dueDate !== 'N/A' && new Date(dueDate) < new Date()) {\n        task.style.border = '2px solid red';\n    }\n    task.draggable = true;\n    task.addEventListener('dragstart', dragStart);\n    task.querySelector('.delete-btn').addEventListener('click', deleteTask);\n    task.querySelector('.priority').addEventListener('click', togglePriority);\n    updateTaskCount();\n};\n\nconst updateTaskCount = () => {\n    const todoCount = document.getElementById('todo-count');\n    const inProgressCount = document.getElementById('in-progress-count');\n    const doneCount = document.getElementById('done-count');\n    todoCount.textContent = document.querySelectorAll('.column:nth-child(1) .task').length;\n    inProgressCount.textContent = document.querySelectorAll('.column:nth-child(2) .task').length;\n    doneCount.textContent = document.querySelectorAll('.column:nth-child(3) .task').length;\n};